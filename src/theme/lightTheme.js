import { colors } from './colors';
import { spacing } from './spacing';
import { typography } from './typography';

export const lightTheme = {
  dark: false,
  colors: {
    primary: colors.verdantGreen,
    background: colors.greentintedWhite,
    surface: colors.lightClay,
    text: colors.darkGreen,
    textSecondary: colors.charcoalGreen,
    accent: colors.brightLeafGreen,
    border: colors.clay,
    notification: colors.teal,
    card: colors.warmBeige,
    error: colors.error,
    success: colors.success,
    warning: colors.warning,
    info: colors.info,
  },
  spacing,
  typography,
};